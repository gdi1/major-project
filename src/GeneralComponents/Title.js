import styled from "styled-components";
import text_styles from "../style-utils/text_styles";

const Title = styled.h1`
  font-size: ${text_styles.fonts.med};
  font-weight: bold;
  font-family: ${text_styles.styles.fontFamily};
`;
export default Title;
